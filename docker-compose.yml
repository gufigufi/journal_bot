version: '3.8'

services:
  grades-bot:
    build: .
    container_name: grades-bot
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "8081:8081"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/grades.db
